public class Interface {
    }
    interface OperacoesCafeteria {
        void adicionarProduto(String produto, int quantidade);
        double verificarSaldo();
        void finalizarTransacao();
        boolean verificarDisponibilidade(String produto, int quantidade);
        boolean verificarCliente(int cpf);
        void confirmarEntrega();
    }
    Agora, você pode implementar essa interface nas suas classes existentes:

    java
    Copy code
    class Funcionario extends Pessoa implements OperacoesCafeteria {

    }

    class DadosCliente extends Pessoa implements OperacoesCafeteria {

    }

    class Estoque implements OperacoesCafeteria {

    }

    class Delivery implements OperacoesCafeteria {

    }

    class Orcamento implements OperacoesCafeteria {
        
    }
}
